# Ruff linter configuration for AI Capital MVP
target-version = "py311"
line-length = 120

[lint]
# Only check for critical errors in MVP phase
select = ["F811", "F821", "E999"]
# F811 = redefined unused name
# F821 = undefined name
# E999 = syntax error
