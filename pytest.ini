[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
asyncio_mode = auto
addopts = -v --tb=short --strict-markers
markers =
    auth: тесты аутентификации
    health: тесты health-check
    rates: тесты курсов валют
